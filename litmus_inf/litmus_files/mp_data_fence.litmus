C mp_data_fence

{ int x=0; int y=0; int z=0; }

P0(
  int* x, int* y, int* z
) {
  int r2 = READ_ONCE(*z);
  smp_mb();
  WRITE_ONCE(*x, 1);
  smp_mb();
  WRITE_ONCE(*y, 1);
}

P1(
  int* x, int* y
) {
  int r0 = READ_ONCE(*y);
  smp_mb();
  int r1 = READ_ONCE(*x);
}

exists (1:r0=1 /\ 1:r1=0)
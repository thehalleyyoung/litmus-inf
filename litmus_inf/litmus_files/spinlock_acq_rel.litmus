C spinlock_acq_rel

{ int x=0; int y=0; }

P0(
  int* x, int* y
) {
  int r0 = READ_ONCE(*y);
  WRITE_ONCE(*y, 1);
  WRITE_ONCE(*x, 1);
  WRITE_ONCE(*y, 0);
}

P1(
  int* x, int* y
) {
  int r1 = READ_ONCE(*y);
  WRITE_ONCE(*y, 1);
  int r2 = READ_ONCE(*x);
}

exists (1:r1=0 /\ 1:r2=0)
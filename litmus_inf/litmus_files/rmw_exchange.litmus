C rmw_exchange

{ int x=0; int y=0; }

P0(
  int* x, int* y
) {
  WRITE_ONCE(*x, 1);
  int r0 = READ_ONCE(*y);
  WRITE_ONCE(*y, 1);
}

P1(
  int* x, int* y
) {
  WRITE_ONCE(*y, 2);
  int r1 = READ_ONCE(*y);
  int r2 = READ_ONCE(*x);
}

exists (0:r0=0 /\ 1:r2=0)
C lockfree_stack_pop

{ int x=0; int y=0; }

P0(
  int* x, int* y
) {
  int r0 = READ_ONCE(*y);
  int r1 = READ_ONCE(*x);
  WRITE_ONCE(*y, 1);
}

P1(
  int* x, int* y
) {
  WRITE_ONCE(*x, 2);
  WRITE_ONCE(*y, 2);
}

exists (0:r0=0 /\ 0:r1=2)
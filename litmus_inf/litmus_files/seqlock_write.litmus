C seqlock_write

{ int x=0; int y=0; }

P0(
  int* x, int* y
) {
  WRITE_ONCE(*y, 1);
  WRITE_ONCE(*x, 1);
  WRITE_ONCE(*y, 2);
}

P1(
  int* x, int* y
) {
  int r0 = READ_ONCE(*y);
  int r1 = READ_ONCE(*x);
}

exists (1:r0=2 /\ 1:r1=0)
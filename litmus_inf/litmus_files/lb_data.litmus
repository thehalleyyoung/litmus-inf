C lb_data

{ int x=0; int y=0; }

P0(
  int* x, int* y
) {
  int r0 = READ_ONCE(*x);
  WRITE_ONCE(*y, 1);
}

P1(
  int* x, int* y
) {
  int r1 = READ_ONCE(*y);
  WRITE_ONCE(*x, 1);
}

exists (0:r0=1 /\ 1:r1=1)
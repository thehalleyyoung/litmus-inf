C epoch_reclaim

{ int x=0; int y=0; }

P0(
  int* x, int* y
) {
  WRITE_ONCE(*y, 1);
  int r0 = READ_ONCE(*x);
  WRITE_ONCE(*y, 2);
}

P1(
  int* x, int* y
) {
  WRITE_ONCE(*x, 2);
  int r1 = READ_ONCE(*y);
}

exists (0:r0=2 /\ 1:r1=1)
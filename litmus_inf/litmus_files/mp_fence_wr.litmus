C mp_fence_wr

{ int x=0; int y=0; }

P0(
  int* x, int* y
) {
  WRITE_ONCE(*x, 1);
  smp_mb();
  WRITE_ONCE(*y, 1);
}

P1(
  int* x, int* y
) {
  int r0 = READ_ONCE(*y);
  smp_mb();
  int r1 = READ_ONCE(*x);
}

exists (1:r0=1 /\ 1:r1=0)
C gpu_sb_scope_mismatch

{ int x=0; int y=0; }

P0(
  int* x, int* y
) {
  WRITE_ONCE(*x, 1);
  smp_mb();
  int r0 = READ_ONCE(*y);
}

P1(
  int* x, int* y
) {
  WRITE_ONCE(*y, 1);
  smp_mb();
  int r1 = READ_ONCE(*x);
}

exists (0:r0=0 /\ 1:r1=0)
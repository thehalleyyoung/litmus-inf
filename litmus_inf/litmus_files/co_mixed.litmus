C co_mixed

{ int x=0; }

P0(
  int* x
) {
  WRITE_ONCE(*x, 1);
}

P1(
  int* x
) {
  int r0 = READ_ONCE(*x);
  int r1 = READ_ONCE(*x);
}

P2(
  int* x
) {
  WRITE_ONCE(*x, 2);
}

exists (1:r0=2 /\ 1:r1=1)
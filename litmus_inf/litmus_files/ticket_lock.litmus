C ticket_lock

{ int x=0; int y=0; int z=0; }

P0(
  int* x, int* y, int* z
) {
  int r0 = READ_ONCE(*y);
  WRITE_ONCE(*y, 1);
  int r1 = READ_ONCE(*z);
  WRITE_ONCE(*x, 1);
}

P1(
  int* x, int* z
) {
  int r2 = READ_ONCE(*x);
  WRITE_ONCE(*z, 1);
}

exists (0:r1=0 /\ 1:r2=1)
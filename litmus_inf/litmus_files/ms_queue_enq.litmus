C ms_queue_enq

{ int x=0; int y=0; int z=0; }

P0(
  int* x, int* y, int* z
) {
  WRITE_ONCE(*x, 1);
  WRITE_ONCE(*z, 1);
  int r0 = READ_ONCE(*y);
  WRITE_ONCE(*y, 1);
}

P1(
  int* x, int* y
) {
  int r1 = READ_ONCE(*y);
  int r2 = READ_ONCE(*x);
}

exists (1:r1=1 /\ 1:r2=0)